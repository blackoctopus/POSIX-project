#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, string argv[])
{
	string k = argv[1];
}
	
	int k = atoi(argv[1]);
		for (int i = 0, n = strlen(p); i < n; i++)
		{
			printf("%c", p[i]);
		}